<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MuscleMemory.ViewModels"
             xmlns:models="clr-namespace:MuscleMemory.Models"
             x:Class="MuscleMemory.Views.WorkoutHistoryPage"
             x:DataType="viewmodels:WorkoutHistoryViewModel"
             Title="{Binding WorkoutName}"
             BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#121212}">

    <Grid>
        <VerticalStackLayout HorizontalOptions="Center" 
                             VerticalOptions="Center" 
                             Spacing="20"
                             IsVisible="{Binding IsEmpty}">
            <Label Text="No history available for this workout yet.&#10;Complete a session to see your progress!" 
                   TextColor="{AppThemeBinding Light=#99000000, Dark=#B3FFFFFF}" 
                   FontSize="18" 
                   FontFamily="LilitaOne"
                   HorizontalTextAlignment="Center"/>
        </VerticalStackLayout>
        
        <CollectionView ItemsSource="{Binding Sessions}" Margin="20">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:WorkoutHistorySession">
                    <Border Margin="0,0,0,15" Padding="15" Stroke="{AppThemeBinding Light=#99000000, Dark=#66FFFFFF}" StrokeThickness="1">
                        <Border.Shadow>
                            <Shadow Brush="#000000" Offset="0,4" Opacity="0.15" Radius="10"/>
                        </Border.Shadow>
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="16" />
                        </Border.StrokeShape>
                        <VerticalStackLayout Spacing="10">
                                                        <Grid ColumnDefinitions="*, Auto">
                                <Label Grid.Column="0" Text="{Binding StartTime, StringFormat='{0:MMM dd, yyyy}'}" TextColor="{StaticResource PrimaryRed}" FontSize="18" FontFamily="LilitaOne" />
                                <Label Grid.Column="1" Text="{Binding Duration, StringFormat='{0:hh\\:mm\\:ss}'}" TextColor="{AppThemeBinding Light=#99000000, Dark=#B3FFFFFF}" FontSize="16" />
                            </Grid>
                            <Label Text="{Binding TotalVolume, StringFormat='Total Volume: {0} kg'}" TextColor="{AppThemeBinding Light=#000000, Dark=#F5F5F7}" FontSize="16" />
                            
                                                        <VerticalStackLayout BindableLayout.ItemsSource="{Binding Exercises}">
                                <BindableLayout.ItemTemplate>
                                    <DataTemplate x:DataType="models:WorkoutHistoryExercise">
                                        <VerticalStackLayout Spacing="5" Margin="0,10,0,0">
                                            <Label Text="{Binding ExerciseName}" TextColor="{AppThemeBinding Light=#000000, Dark=#F5F5F7}" FontSize="16" FontFamily="LilitaOne" />
                                                                                        <Border Stroke="#E0E0E0" StrokeThickness="1" BackgroundColor="#FAFAFA">
                                                <Border.StrokeShape>
                                                    <RoundRectangle CornerRadius="12" />
                                                </Border.StrokeShape>
                                                <VerticalStackLayout>
                                                    <Grid ColumnDefinitions="*,*,*" Padding="5" BackgroundColor="#EEEEEE">
                                                        <Label Grid.Column="0" Text="Set" TextColor="{AppThemeBinding Light=#000000, Dark=#F5F5F7}" FontSize="12" FontFamily="LilitaOne" HorizontalTextAlignment="Center" />
                                                        <Label Grid.Column="1" Text="Weight" TextColor="{AppThemeBinding Light=#000000, Dark=#F5F5F7}" FontSize="12" FontFamily="LilitaOne" HorizontalTextAlignment="Center" />
                                                        <Label Grid.Column="2" Text="Reps" TextColor="{AppThemeBinding Light=#000000, Dark=#F5F5F7}" FontSize="12" FontFamily="LilitaOne" HorizontalTextAlignment="Center" />
                                                    </Grid>
                                                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding Sets}">
                                                        <BindableLayout.ItemTemplate>
                                                            <DataTemplate x:DataType="models:WorkoutSet">
                                                                <Grid ColumnDefinitions="*,*,*" Padding="5" BackgroundColor="Transparent">
                                                                    <Label Grid.Column="0" Text="{Binding SetNumber}" TextColor="{AppThemeBinding Light=#000000, Dark=#F5F5F7}" FontSize="14" HorizontalTextAlignment="Center" />
                                                                    <Label Grid.Column="1" Text="{Binding Weight}" TextColor="{AppThemeBinding Light=#000000, Dark=#F5F5F7}" FontSize="14" HorizontalTextAlignment="Center" />
                                                                    <Label Grid.Column="2" Text="{Binding Reps}" TextColor="{AppThemeBinding Light=#000000, Dark=#F5F5F7}" FontSize="14" HorizontalTextAlignment="Center" />
                                                                </Grid>
                                                            </DataTemplate>
                                                        </BindableLayout.ItemTemplate>
                                                    </VerticalStackLayout>
                                                </VerticalStackLayout>
                                            </Border>
                                        </VerticalStackLayout>
                                    </DataTemplate>
                                </BindableLayout.ItemTemplate>
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>

